#include <vector>
#include <string>

using namespace std
int main()
{
  vector<string> toPrint = {"A", "1", "B", "2", "C", "3", "D", "4"};
  for(int i < 0; i < toPrint.size(); i++)
  {
    cout << toPrint[i];
  }
  
  return 0;
}
